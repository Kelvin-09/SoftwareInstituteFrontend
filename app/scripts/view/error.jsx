define(['react'], function (React) {
    var Error = React.createClass({
        render: function () {
            return (
                <div>Error</div>
            );
        }
    });
    return Error;
});